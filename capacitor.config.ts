import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hub3.angular',
  appName: 'hub3-angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
